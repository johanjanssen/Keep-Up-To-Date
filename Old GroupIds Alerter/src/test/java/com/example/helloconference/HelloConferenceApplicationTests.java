package com.example.helloconference;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
class HelloConferenceApplicationTests {

    @Test
    void contextLoads() {
        // Just proves HelloConference — same app as ../Vulnerable Application Old Java/ —
        // still boots with the old-groupid dependencies declared alongside it.
    }
}
